module.exports = {
  stories: ['../src/**/*.example.js'],
  addons: [
    '@storybook/addon-knobs/register',
    '@storybook/addon-actions/register',
    '@storybook/addon-options/register',
    '@storybook/addon-links/register',
  ],
}
